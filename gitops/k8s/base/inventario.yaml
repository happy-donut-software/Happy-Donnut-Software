apiVersion: apps/v1
kind: Deployment
metadata:
  name: servicio-inventario
  labels: {app.kubernetes.io/name: servicio-inventario, app.kubernetes.io/part-of: happy-donut, observability.happy-donut/metrics: "true"}
spec:
  replicas: 2
  selector: {matchLabels: {app.kubernetes.io/name: servicio-inventario}}
  template:
    metadata:
      labels: {app.kubernetes.io/name: servicio-inventario, app.kubernetes.io/part-of: happy-donut, observability.happy-donut/metrics: "true"}
    spec:
      securityContext: {runAsNonRoot: true, runAsUser: 82, runAsGroup: 82, seccompProfile: {type: RuntimeDefault}}
      containers:
        - name: app
          image: ghcr.io/happy-donut-software/happy-donnut-software/inventario:develop
          imagePullPolicy: IfNotPresent
          command: ["php", "artisan", "serve", "--host=0.0.0.0", "--port=8000"]
          ports: [{name: http, containerPort: 8000}]
          envFrom:
            - configMapRef: {name: happy-donut-config}
            - secretRef: {name: happy-donut-secrets}
          env:
            - {name: DB_HOST, value: bd-inventario}
            - {name: DB_DATABASE, value: inventario_db}
            - {name: SERVICE_NAME, value: "inventario"}
            - {name: OTEL_SERVICE_NAME, value: "inventario"}
          readinessProbe: {httpGet: {path: /up, port: http}, initialDelaySeconds: 20, timeoutSeconds: 5, periodSeconds: 10, failureThreshold: 6}
          livenessProbe: {httpGet: {path: /up, port: http}, initialDelaySeconds: 60, timeoutSeconds: 5, periodSeconds: 20}
          resources:
            requests: {cpu: 100m, memory: 192Mi}
            limits: {cpu: 500m, memory: 512Mi}
          securityContext:
            allowPrivilegeEscalation: false
            capabilities: {drop: ["ALL"]}
---
apiVersion: v1
kind: Service
metadata:
  name: servicio-inventario
  labels: {app.kubernetes.io/name: servicio-inventario, app.kubernetes.io/part-of: happy-donut, observability.happy-donut/metrics: "true"}
spec:
  selector: {app.kubernetes.io/name: servicio-inventario}
  ports: [{name: http, port: 80, targetPort: http}]
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: {name: servicio-inventario}
spec:
  minAvailable: 1
  selector: {matchLabels: {app.kubernetes.io/name: servicio-inventario}}
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: {name: servicio-inventario}
spec:
  scaleTargetRef: {apiVersion: apps/v1, kind: Deployment, name: servicio-inventario}
  minReplicas: 2
  maxReplicas: 6
  behavior:
    scaleDown: {stabilizationWindowSeconds: 300}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target: {type: Utilization, averageUtilization: 70}